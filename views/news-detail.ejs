<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= news.title %> - जिला अपडेट</title>

<style>
body{
  margin:0;
  font-family:Arial, sans-serif;
  background:#f4f4f4;
}

/* HEADER */
.header{
  background:linear-gradient(90deg,#c40000,#ff0000);
  color:white;
  text-align:center;
  padding:18px;
}

.container{
  max-width:900px;
  margin:auto;
  padding:15px;
  background:white;
  margin-top:20px;
  border-radius:10px;
  box-shadow:0 4px 15px rgba(0,0,0,0.08);
}

/* TITLE */
.title{
  font-size:26px;
  margin-bottom:10px;
}

/* META */
.meta{
  font-size:14px;
  color:#666;
  margin-bottom:15px;
}

/* IMAGE */
.hero-img{
  width:100%;
  border-radius:8px;
  margin-bottom:15px;
}

/* CONTENT */
.content{
  font-size:18px;
  line-height:1.7;
  color:#333;
  margin-bottom:20px;
}

/* SHARE */
.share{
  margin-top:15px;
}

.share a{
  display:inline-block;
  padding:8px 12px;
  background:#25D366;
  color:white;
  border-radius:6px;
  text-decoration:none;
  font-size:14px;
}

/* VIEWS */
.views{
  margin-top:10px;
  font-size:14px;
  color:#777;
}

/* BACK */
.back{
  display:inline-block;
  margin-top:20px;
  text-decoration:none;
  font-weight:bold;
  color:#c40000;
}

/* MOBILE */
@media(max-width:600px){
  .title{
    font-size:20px;
  }
  .content{
    font-size:16px;
  }
}
</style>
</head>
<body>

<div class="header">
  <h2>जिला अपडेट</h2>
</div>

<div class="container">

  <div class="title">
    <%= news.title %>
  </div>

  <div class="meta">
    📍 <%= news.location || "Location" %> |
    🗓 <%= news.createdAt.toLocaleDateString("en-IN") %> |
    ⏰ <%= news.createdAt.toLocaleTimeString("en-IN") %>
  </div>

  <% if(news.image){ %>
    <img src="<%= news.image %>" class="hero-img">
  <% } %>

  <div class="content">
    <%= news.content %>
  </div>

  <div class="share">
    <a target="_blank"
      href="https://wa.me/?text=<%= encodeURIComponent(news.title + ' - ' + req.protocol + '://' + req.get('host') + req.originalUrl) %>">
      Share on WhatsApp
    </a>
  </div>

  <div class="views">
    👁 <%= news.views %> views
  </div>

  <a href="/" class="back">← Back to Home</a>

</div>

</body>
</html>