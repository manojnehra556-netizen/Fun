<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= news.title %> | जिला अपडेट</title>

<style>
body{
  margin:0;
  font-family:Arial, sans-serif;
  background:#f4f4f4;
}

/* HEADER */
.header{
  background:linear-gradient(90deg,#c40000,#ff0000);
  color:white;
  text-align:center;
  padding:18px;
}

.top-bar{
  background:#111;
  color:white;
  display:flex;
  justify-content:space-between;
  padding:8px 15px;
  font-size:14px;
}

/* CONTAINER */
.container{
  background:white;
  margin:15px;
  padding:18px;
  border-radius:10px;
  box-shadow:0 5px 15px rgba(0,0,0,0.1);
}

/* TITLE */
.container h1{
  margin-top:0;
  font-size:22px;
}

/* META INFO */
.meta{
  font-size:14px;
  color:#777;
  margin-bottom:12px;
}

/* IMAGE */
.container img{
  width:100%;
  border-radius:8px;
  margin:12px 0;
}

/* CONTENT */
.content{
  font-size:16px;
  line-height:1.6;
  color:#333;
}

/* BACK BUTTON */
.btn{
  display:inline-block;
  margin-top:20px;
  padding:8px 14px;
  background:red;
  color:white;
  border-radius:6px;
  text-decoration:none;
}
.btn:hover{
  background:#b30000;
}
</style>
</head>
<body>

<div class="header">
  <h2>जिला अपडेट</h2>
  आपके जिले की हर खबर, सबसे पहले
</div>

<div class="top-bar">
  <div>
    📍 <%= news.location || "India" %>
  </div>
  <div>
    🕒 <%= new Date(news.createdAt).toLocaleString("en-IN") %>
  </div>
</div>

<div class="container">

  <h1><%= news.title %></h1>

  <div class="meta">
    Category: <b><%= news.category %></b> |
    Views: <b><%= news.views %></b>
  </div>

  <% if(news.image){ %>
    <img src="<%= news.image %>">
  <% } %>

  <div class="content">
    <%= news.content %>
  </div>

  <a href="/" class="btn">⬅ Back to Home</a>

</div>

</body>
</html>