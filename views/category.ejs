<!DOCTYPE html>
<html lang="hi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= category %> - किठाना न्यूज</title>

<style>
body{
  margin:0;
  font-family:Arial, sans-serif;
  background:#f4f4f4;
}

/* HEADER */
header{
  background:#c40000;
  color:white;
  text-align:center;
  padding:20px;
}

header h1{
  margin:0;
  font-size:28px;
}

.tagline{
  color:#ffd700;
  font-size:14px;
  margin-top:5px;
}

/* CONTAINER */
.container{
  max-width:1000px;
  margin:20px auto;
  padding:0 15px;
}

/* CATEGORY TITLE */
.category-title{
  font-size:26px;
  color:#c40000;
  border-bottom:3px solid #c40000;
  padding-bottom:5px;
  margin-bottom:20px;
}

/* NEWS CARD */
.card{
  background:white;
  border-radius:8px;
  box-shadow:0 3px 12px rgba(0,0,0,0.1);
  margin-bottom:20px;
  overflow:hidden;
  transition:0.3s;
}

.card:hover{
  transform:translateY(-4px);
}

/* IMAGE */
.card img{
  width:100%;
  height:220px;
  object-fit:cover;
}

/* CONTENT */
.card-content{
  padding:15px;
}

.card-content h3{
  margin:0 0 10px 0;
  font-size:20px;
}

.card-content p{
  font-size:14px;
  color:#555;
  line-height:1.5;
}

.card-content a{
  text-decoration:none;
  color:black;
}

.read-btn{
  color:#c40000;
  font-weight:bold;
  text-decoration:none;
  display:inline-block;
  margin-top:8px;
}

/* BACK */
.back{
  display:inline-block;
  margin-top:15px;
  color:#c40000;
  font-weight:bold;
  text-decoration:none;
}

/* RESPONSIVE */
@media(max-width:600px){
  .card img{
    height:160px;
  }
  .card-content h3{
    font-size:18px;
  }
}
</style>
</head>

<body>

<header>
  <h1>किठाना न्यूज</h1>
  <div class="tagline">सच की आवाज़, आपके साथ</div>
</header>

<div class="container">

  <div class="category-title">
    📰 <%= category %> News
  </div>

  <% categoryNews.forEach(item => { %>
    <div class="card">

      <% if(item.image){ %>
        <img src="<%= item.image %>">
      <% } %>

      <div class="card-content">
        <a href="/news/<%= item._id %>">
          <h3><%= item.title %></h3>
        </a>

        <p>
          <%= item.content.substring(0,150) %>...
        </p>

        <a class="read-btn" href="/news/<%= item._id %>">
          Read Full News →
        </a>
      </div>

    </div>
  <% }) %>

  <a class="back" href="/">← Back to Home</a>

</div>

</body>
</html>