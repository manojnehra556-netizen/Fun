<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>जिला अपडेट</title>

<style>
body{
  margin:0;
  font-family:Arial;
  background:#f4f4f4;
}

/* HEADER */
.header{
  background:linear-gradient(90deg,#c40000,#ff0000);
  color:white;
  text-align:center;
  padding:18px;
}

/* DATE TIME BAR */
.top-bar{
  background:#111;
  color:white;
  display:flex;
  justify-content:space-between;
  padding:8px 15px;
  font-size:14px;
}

/* PREMIUM CATEGORY */
.category-bar{
  background:white;
  display:flex;
  gap:12px;
  padding:12px;
  overflow-x:auto;
  box-shadow:0 4px 10px rgba(0,0,0,0.08);
}
.category-bar a{
  text-decoration:none;
  padding:8px 18px;
  border-radius:30px;
  background:#f1f1f1;
  font-weight:bold;
  color:#333;
  transition:0.3s;
  white-space:nowrap;
}
.category-bar a:hover{
  background:red;
  color:white;
}

/* BREAKING TICKER */
.breaking{
  display:flex;
  background:black;
  color:white;
  overflow:hidden;
  align-items:center;
}
.breaking-label{
  background:red;
  padding:8px 12px;
  font-weight:bold;
}
.breaking-text{
  white-space:nowrap;
  display:inline-block;
  padding-left:100%;
  animation:scroll 20s linear infinite;
}
@keyframes scroll{
  0%{transform:translateX(0);}
  100%{transform:translateX(-100%);}
}

/* HERO CARD */
.hero{
  background:white;
  margin:15px;
  padding:15px;
  border-radius:10px;
  box-shadow:0 4px 10px rgba(0,0,0,0.1);
}
.hero img{
  width:100%;
  border-radius:6px;
}
.hero h2{
  margin:12px 0;
}
.btn{
  display:inline-block;
  background:red;
  color:white;
  padding:8px 14px;
  border-radius:6px;
  text-decoration:none;
}

</style>
</head>
<body>

<div class="header">
  <h2>जिला अपडेट</h2>
  आपके जिले की हर खबर, सबसे पहले
</div>

<div class="top-bar">
  <div id="date"></div>
  <div id="time"></div>
</div>

<!-- CATEGORY -->
<div class="category-bar">
  <a href="/category/Local">Local</a>
  <a href="/category/Sports">Sports</a>
  <a href="/category/Business">Business</a>
  <a href="/category/Politics">Politics</a>
</div>

<!-- BREAKING TICKER -->
<div class="breaking">
  <div class="breaking-label">BREAKING</div>
  <div class="breaking-text">
    <% if(tickerNews && tickerNews.length > 0){ %>
      <% tickerNews.forEach(item=>{ %>
        <span>
          <a href="/news/<%= item._id %>" style="color:white;text-decoration:none;">
            <%= item.title %>
          </a>
          &nbsp;&nbsp;&nbsp;&nbsp;
        </span>
      <% }) %>
    <% } %>
  </div>
</div>

<!-- HERO BREAKING NEWS LIST -->
<% if(breakingNews && breakingNews.length > 0){ %>

  <% breakingNews.forEach(item=>{ %>

  <div class="hero">

    <% if(item.image){ %>
      <img src="<%= item.image %>">
    <% } %>

    <h2>
      <a href="/news/<%= item._id %>" style="text-decoration:none;color:#000;">
        <%= item.title %>
      </a>
    </h2>

    <a href="/news/<%= item._id %>" class="btn">
      Read Full News →
    </a>

  </div>

  <% }) %>

<% } %>

<script>
function updateTime(){
  const now=new Date();
  document.getElementById("date").innerText=
    "📅 "+now.toLocaleDateString("en-IN");
  document.getElementById("time").innerText=
    "⏰ "+now.toLocaleTimeString("en-IN");
}
setInterval(updateTime,1000);
updateTime();
</script>

</body>
</html>