<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>जिला अपडेट</title>

<style>
*{margin:0;padding:0;box-sizing:border-box;}

body{
  font-family:Arial, sans-serif;
  background:#f4f4f4;
}

/* HEADER */
.header{
  background:linear-gradient(90deg,#c40000,#ff0000);
  color:white;
  text-align:center;
  padding:20px;
}
.header h1{margin:0;}
.header p{margin-top:5px;font-size:14px;}

/* DATE BAR */
.top-bar{
  background:#111;
  color:white;
  display:flex;
  justify-content:space-between;
  padding:8px 15px;
  font-size:14px;
}

/* BREAKING TICKER */
.breaking{
  background:black;
  color:white;
  display:flex;
  overflow:hidden;
}
.breaking-label{
  background:red;
  padding:8px 12px;
  font-weight:bold;
}
.breaking-text{
  white-space:nowrap;
  display:inline-block;
  padding-left:100%;
  animation: scroll 18s linear infinite;
}
@keyframes scroll{
  0%{ transform:translateX(0); }
  100%{ transform:translateX(-100%); }
}

/* MAIN BREAKING */
.main-breaking{
  background:white;
  margin:15px;
  padding:15px;
  border-radius:8px;
  box-shadow:0 3px 8px rgba(0,0,0,0.1);
  text-align:center;
}
.main-breaking img{
  width:100%;
  max-height:250px;
  object-fit:cover;
  border-radius:6px;
}
.main-breaking h2{
  margin-top:15px;
  font-size:22px;
}
.btn{
  display:inline-block;
  margin-top:15px;
  padding:8px 15px;
  background:#c40000;
  color:white;
  text-decoration:none;
  border-radius:4px;
}

/* CATEGORY BAR */
.category-bar{
  display:flex;
  justify-content:space-around;
  background:white;
  margin:15px;
  padding:12px;
  border-radius:8px;
  box-shadow:0 2px 6px rgba(0,0,0,0.1);
}
.category-bar a{
  text-decoration:none;
  font-size:18px;
  font-weight:bold;
  color:#c40000;
  padding:8px 12px;
  border-radius:6px;
  transition:0.3s;
}
.category-bar a:hover{
  background:#c40000;
  color:white;
}

/* CATEGORY NEWS */
.section{
  margin:15px;
}
.section h3{
  border-left:4px solid red;
  padding-left:10px;
  margin-bottom:10px;
}
.card{
  background:white;
  padding:10px;
  margin-bottom:10px;
  border-radius:6px;
  box-shadow:0 2px 5px rgba(0,0,0,0.08);
}
.card a{
  text-decoration:none;
  font-weight:bold;
  color:black;
}

/* MOBILE */
@media(max-width:600px){
  .main-breaking h2{font-size:18px;}
  .category-bar{flex-wrap:wrap;gap:10px;}
}
</style>
</head>

<body>

<!-- HEADER -->
<div class="header">
  <h1>जिला अपडेट</h1>
  <p>आपके जिले की हर खबर, सबसे पहले</p>
</div>

<!-- DATE BAR -->
<div class="top-bar">
  <div id="currentDate"></div>
  <div id="liveClock"></div>
</div>

<!-- BREAKING TICKER -->
<div class="breaking">
  <div class="breaking-label">BREAKING</div>
  <div class="breaking-text">
    <% if(breakingNews){ %>
      <%= breakingNews.title %> | 
    <% } %>
  </div>
</div>

<!-- MAIN BREAKING -->
<% if(breakingNews){ %>
<div class="main-breaking">

  <% if(breakingNews.image){ %>
    <img src="<%= breakingNews.image %>">
  <% } %>

  <h2><%= breakingNews.title %></h2>

  <a href="/news/<%= breakingNews._id %>" class="btn">
    Read Full News →
  </a>

</div>
<% } %>

<!-- CATEGORY BAR -->
<div class="category-bar">
  <a href="/category/Local">Local</a>
  <a href="/category/Sports">Sports</a>
  <a href="/category/Business">Business</a>
  <a href="/category/Politics">Politics</a>
</div>

<!-- LOCAL -->
<div class="section">
<h3>Local News</h3>
<% localNews.forEach(item=>{ %>
  <div class="card">
    <a href="/news/<%= item._id %>"><%= item.title %></a>
  </div>
<% }) %>
</div>

<!-- SPORTS -->
<div class="section">
<h3>Sports News</h3>
<% sportsNews.forEach(item=>{ %>
  <div class="card">
    <a href="/news/<%= item._id %>"><%= item.title %></a>
  </div>
<% }) %>
</div>

<!-- BUSINESS -->
<div class="section">
<h3>Business News</h3>
<% businessNews.forEach(item=>{ %>
  <div class="card">
    <a href="/news/<%= item._id %>"><%= item.title %></a>
  </div>
<% }) %>
</div>

<!-- POLITICS -->
<div class="section">
<h3>Politics News</h3>
<% politicsNews.forEach(item=>{ %>
  <div class="card">
    <a href="/news/<%= item._id %>"><%= item.title %></a>
  </div>
<% }) %>
</div>

<!-- CLOCK SCRIPT -->
<script>
function updateClock(){
  const now=new Date();
  document.getElementById("currentDate").innerHTML=
  "📅 "+now.toLocaleDateString("en-IN");
  document.getElementById("liveClock").innerHTML=
  "⏰ "+now.toLocaleTimeString("en-IN");
}
setInterval(updateClock,1000);
updateClock();
</script>

</body>
</html>