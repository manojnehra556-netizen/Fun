<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>जिला अपडेट</title>

<style>
body{
  margin:0;
  font-family: Arial, sans-serif;
  background:#f4f4f4;
}

/* Header */
.header{
  background:linear-gradient(90deg,#c40000,#ff0000);
  color:white;
  text-align:center;
  padding:15px;
}
.header h1{
  margin:0;
}
.tagline{
  font-size:14px;
}

/* Top Date Bar */
.top-bar{
  background:#111;
  color:#fff;
  display:flex;
  justify-content:space-between;
  padding:8px 15px;
  font-size:14px;
}

/* Breaking Big */
.main-breaking{
  background:#fff;
  margin:15px;
  padding:15px;
  border-radius:8px;
  box-shadow:0 2px 8px rgba(0,0,0,0.1);
}

.main-breaking h2{
  font-size:20px;
  margin:10px 0;
}

.breaking-img{
  width:100%;
  border-radius:6px;
}

.read-btn{
  display:inline-block;
  background:#e60000;
  color:white;
  padding:8px 15px;
  text-decoration:none;
  border-radius:5px;
  margin-top:10px;
}

/* Breaking Ticker */
.breaking-bar{
  display:flex;
  align-items:center;
  background:black;
  color:white;
  overflow:hidden;
}

.breaking-label{
  background:red;
  padding:8px 12px;
  font-weight:bold;
}

.breaking-text{
  white-space:nowrap;
  display:inline-block;
  padding-left:100%;
  animation:scroll 15s linear infinite;
}

@keyframes scroll{
  0%{ transform:translateX(0); }
  100%{ transform:translateX(-100%); }
}

/* Mobile */
@media(max-width:600px){
  .main-breaking h2{
    font-size:18px;
  }
}
</style>
</head>

<body>

<!-- Header -->
<div class="header">
  <h1>जिला अपडेट</h1>
  <div class="tagline">आपके जिले की हर खबर, सबसे पहले</div>
</div>

<!-- Date + Live Time -->
<div class="top-bar">
  <div id="currentDate"></div>
  <div id="liveClock"></div>
</div>

<!-- BIG BREAKING NEWS -->
<% if (breakingNews) { %>
<div class="main-breaking">

  <% if (breakingNews.image) { %>
    <img src="<%= breakingNews.image %>" class="breaking-img">
  <% } %>

  <h2><%= breakingNews.title %></h2>

  <a href="/news/<%= breakingNews._id %>" class="read-btn">
    Read Full News →
  </a>

</div>
<% } %>

<!-- MOVING BREAKING TICKER -->
<% if (tickerNews && tickerNews.length > 0) { %>
<div class="breaking-bar">

  <div class="breaking-label">BREAKING</div>

  <div class="breaking-text">
    <% tickerNews.forEach(item => { %>
      <span>
        <%= item.title %> &nbsp;&nbsp;&nbsp;
      </span>
    <% }) %>
  </div>

</div>
<% } %>

<!-- JS Date & Clock -->
<script>
function updateClock(){
  const now = new Date();
  document.getElementById("currentDate").innerHTML =
    "📅 " + now.toLocaleDateString("en-IN");

  document.getElementById("liveClock").innerHTML =
    "⏰ " + now.toLocaleTimeString("en-IN");
}
setInterval(updateClock,1000);
updateClock();
</script>

</body>
</html>