<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>किठाना न्यूज</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
body{margin:0;font-family:Arial;background:#f4f4f4}
header{background:#c40000;color:white;text-align:center;padding:15px}
.tagline{color:#ffd700;font-size:14px}
.breaking-bar{background:black;color:white;padding:8px;overflow:hidden;white-space:nowrap}
.breaking-label{background:red;padding:4px 8px;font-weight:bold;animation:blink 1s infinite}
@keyframes blink{50%{opacity:0}}
.breaking-scroll{display:inline-block;animation:scroll 15s linear infinite}
@keyframes scroll{0%{transform:translateX(100%)}100%{transform:translateX(-100%)}}
.section{padding:15px}
.section h2{color:#c40000;border-bottom:2px solid #c40000}
.card{background:white;padding:10px;margin:10px 0;border-radius:6px;box-shadow:0 2px 6px rgba(0,0,0,0.1)}
.card a{text-decoration:none;color:black}
.read-more{color:red;font-weight:bold;text-decoration:none}
</style>
</head>

<body>

<header>
<h1>किठाना न्यूज</h1>
<div class="tagline">सच की आवाज़, आपके साथ</div>
</header>

<div class="breaking-bar">
<span class="breaking-label">🔴 BREAKING</span>
<div class="breaking-scroll">
<% breakingNews.forEach(item => { %>
<a href="/news/<%= item._id %>" style="color:white;margin-right:40px;">
<%= item.title %>
</a>
<% }) %>
</div>
</div>

<div class="section">
<h2>🔥 TOP NEWS</h2>
<% topNews.forEach(item => { %>
<div class="card">
<a href="/news/<%= item._id %>"><h3><%= item.title %></h3></a>
</div>
<% }) %>
</div>

<!-- Example Local Section -->
<div class="section">
<h2>🏘 Local News</h2>
<% local.forEach(item => { %>
<div class="card">
<a href="/news/<%= item._id %>"><h3><%= item.title %></h3></a>
</div>
<% }) %>
<a class="read-more" href="/category/Local">Read More →</a>
</div>

</body>
</html>