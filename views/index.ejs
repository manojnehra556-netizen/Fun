<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>जिला अपडेट</title>

<style>

body{
margin:0;
font-family:Arial;
background:#f4f4f4;
}

/* HEADER */
.header{
background:linear-gradient(90deg,#c40000,#ff0000);
color:white;
text-align:center;
padding:20px;
}

/* DATE BAR */
.topbar{
background:#111;
color:white;
display:flex;
justify-content:space-between;
padding:8px 15px;
font-size:14px;
}

/* BREAKING TICKER */
.breaking{
background:black;
color:white;
display:flex;
align-items:center;
overflow:hidden;
}

.breaking-label{
background:red;
padding:8px 15px;
font-weight:bold;
}

.breaking-text{
white-space:nowrap;
display:inline-block;
padding-left:100%;
animation:scroll 15s linear infinite;
}

@keyframes scroll{
0%{transform:translateX(0);}
100%{transform:translateX(-100%);}
}

/* CATEGORY NAV */
.nav{
display:flex;
justify-content:space-around;
background:#fff;
padding:12px;
font-weight:bold;
border-bottom:2px solid #eee;
}

.nav a{
text-decoration:none;
color:#c40000;
}

/* BIG BREAKING CARD */
.hero{
background:white;
margin:15px;
padding:15px;
border-radius:8px;
box-shadow:0 2px 6px rgba(0,0,0,0.1);
}

.hero img{
width:100%;
border-radius:8px;
margin-bottom:10px;
}

.hero h2{
margin:10px 0;
font-size:20px;
}

.btn{
display:inline-block;
background:#c40000;
color:white;
padding:8px 15px;
border-radius:5px;
text-decoration:none;
margin-top:10px;
}

</style>
</head>

<body>

<div class="header">
<h1>जिला अपडेट</h1>
<div>आपके जिले की हर खबर, सबसे पहले</div>
</div>

<div class="topbar">
<div id="date"></div>
<div id="time"></div>
</div>

<div class="breaking">
<div class="breaking-label">BREAKING</div>
<div class="breaking-text">
<% tickerNews.forEach(item => { %>
<%= item.title %> &nbsp;&nbsp;&nbsp;
<% }) %>
</div>
</div>

<div class="nav">
<a href="/">Home</a>
<a href="/category/Local">Local</a>
<a href="/category/Sports">Sports</a>
<a href="/category/Business">Business</a>
<a href="/category/Politics">Politics</a>
</div>

<% if(breaking){ %>
<div class="hero">

<% if(breaking.image){ %>
<img src="<%= breaking.image %>">
<% } %>

<h2><%= breaking.title %></h2>

<p><%= breaking.content.substring(0,150) %>...</p>

<a class="btn" href="/news/<%= breaking._id %>">
Read Full News →
</a>

</div>
<% } %>

<script>
function updateTime(){
let now=new Date();
document.getElementById("date").innerText=
now.toLocaleDateString("en-IN");
document.getElementById("time").innerText=
now.toLocaleTimeString("en-IN");
}
setInterval(updateTime,1000);
updateTime();
</script>

</body>
</html>