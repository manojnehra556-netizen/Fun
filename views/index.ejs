<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>जिला अपडेट</title>

<style>
body{
  margin:0;
  font-family:Arial, sans-serif;
  background:#f5f5f5;
}

/* HEADER */
.header{
  background:linear-gradient(90deg,#c40000,#ff0000);
  color:white;
  text-align:center;
  padding:18px;
}
.header h1{margin:0;font-size:26px;}

/* BREAKING CONTAINER */
.breaking-container{
  max-width:900px;
  margin:20px auto;
  background:white;
  border-radius:10px;
  overflow:hidden;
  box-shadow:0 4px 15px rgba(0,0,0,0.1);
}

/* IMAGE */
.breaking-img{
  width:100%;
  height:300px;
  object-fit:cover;
}

/* CONTENT */
.breaking-content{
  padding:20px;
}

.breaking-label{
  background:red;
  color:white;
  padding:5px 10px;
  display:inline-block;
  font-weight:bold;
  margin-bottom:10px;
}

.breaking-title{
  font-size:26px;
  font-weight:bold;
  margin-bottom:10px;
}

.breaking-preview{
  color:#555;
  margin-bottom:15px;
}

.read-btn{
  background:#c40000;
  color:white;
  padding:10px 15px;
  text-decoration:none;
  border-radius:5px;
}

/* MOBILE */
@media(max-width:768px){
  .breaking-title{font-size:20px;}
  .breaking-img{height:200px;}
}
</style>
</head>

<body>

<div class="header">
  <h1>जिला अपडेट</h1>
  <p>आपके जिले की हर खबर, सबसे पहले</p>
</div>

<% if(breakingNews && breakingNews.length > 0){ 
   const news = breakingNews[0]; %>

<div class="breaking-container">

  <% if(news.image){ %>
    <img src="<%= news.image %>" class="breaking-img">
  <% } %>

  <div class="breaking-content">

    <div class="breaking-label">BREAKING NEWS</div>

    <div class="breaking-title">
      <%= news.title %>
    </div>

    <div class="breaking-preview">
      <%= news.content.substring(0,200) %>...
    </div>

    <a class="read-btn" href="/news/<%= news._id %>">
      Read Full News →
    </a>

  </div>
</div>

<% } else { %>

<div style="text-align:center;margin-top:50px;">
  <h2>No Breaking News Available</h2>
</div>

<% } %>

</body>
</html>